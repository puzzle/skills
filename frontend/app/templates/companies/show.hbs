{{component (if personEditing 'company-edit' 'person-show' )
            person=model
            submit=(action (toggle 'personEditing' this))
            model-header-title = 'Personalien'
            personEditing=(action (toggle 'personEditing' this))
}}

{{company-show company=model}}
{{#power-select
  triggerId="people-search"
  selected=selected
  options=peopleToSelect
  searchEnabled=true
  searchField="name"
  placeholder="Person suchen"
  onfocus=(action "handleFocus")
  onblur=(action "handleBlur")
  onchange=(action 'changePerson')
as |person|
}}
  {{person.name}} <small>({{person.company.name}})</small>
{{/power-select}}

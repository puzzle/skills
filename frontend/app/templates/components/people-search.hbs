{{#power-select
  selected=selected
  options=peopleToSelect
  searchEnabled=true
  searchField="name"
  placeholder="Person Suchen"
  onfocus=(action "handleFocus")
  onblur=(action "handleBlur")
  onchange=(action 'changePerson')
as |person|
}}
  {{person.name}}
{{/power-select}}

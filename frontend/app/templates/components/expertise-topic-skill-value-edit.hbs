{{#with (changeset expertiseTopicSkillValue validations) as |etsvChangeset|}}
  <td class="fws-topic-name">{{expertiseTopic.name}}</td>
  <td>
    <input form={{formId}}
        max="80"
        min="0"
        class="form-control"
        type="number"
        value={{expertiseTopicSkillValue.yearsOfExperience}}
        oninput={{action (mut etsvChangeset.yearsOfExperience) value="target.value"}}>
  </td>
  <td>
    <input form={{formId}}
        max="255"
        min="0"
        class="form-control"
        type="number"
        value={{expertiseTopicSkillValue.numberOfProjects}}
        oninput={{action (mut etsvChangeset.numberOfProjects) value="target.value"}}>
  </td>
  <td>
    <input form={{formId}}
        max="4095"
        min="1900"
        class="form-control"
        type="number"
        value={{expertiseTopicSkillValue.lastUse}}
        oninput={{action (mut etsvChangeset.lastUse) value="target.value"}}>
  </td>

  <td>
    <select form={{formId}}
        class=""
        onchange={{action (mut etsvChangeset.skillLevel)
        value="target.value"}}
        required>
      {{#each skillLevelData as |sld|}}
        <option value={{sld}} selected={{eq etsvChangeset.skillLevel sld}}>{{sld}}</option>
      {{/each}}
    </select>
  </td>

  <td>
    <textarea class="form-control"
              rows="1"
        oninput={{action (mut etsvChangeset.comment) value="target.value"}}>
      {{~expertiseTopicSkillValue.comment~}}
    </textarea>
  </td>
  <td>
    <form id={{formId}} onsubmit={{action onsubmit etsvChangeset}}>
      <a href="#collapseFWS{{expertiseCategory.name}}" onclick={{action onsubmit changeset}}>
        <i class="fa save"></i>
      </a>
    </form>
    <form id={{formId}}>
      <a href="#collapseFWS{{expertiseCategory.name}}" onclick={{action cancelEdit changeset}}>
        <i class="fa abort"></i>
      </a>
    </form>
  </td>
{{/with}}

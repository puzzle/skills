
import { documentExportUrl } from 'frontend/helpers/document-export-url';
import { module, test } from 'qunit';

module('Unit | Helper | document export url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = documentExportUrl([42]);
  assert.ok(result);
});


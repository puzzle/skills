import { moduleForModel } from 'ember-qunit';

moduleForModel('employee quantity', 'Unit | Model | employee quantity', {
  // Specify the other units that are required for this test.
  needs: ['model:company']
});

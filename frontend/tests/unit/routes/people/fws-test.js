import { moduleFor } from 'ember-qunit';

moduleFor('route:people/fws', 'Unit | Route | people/fws', {
  // Specify the other units that are required for this test.
  // needs: ['controller:foo']
});


%div.mt-2
  %form
    %input{class: 'form-control w-75', placeholder: 'CVs durchsuchen...', name: 'q', oninput: 'this.form.requestSubmit()'}
  %div.profile-header.mw-100.border-bottom.mt-2
    Suchresultate

  - if @cv_search_results.blank?
    Keine Resultate
  - else
    %div{id: 'search_results'}
      - @cv_search_results.each do |result|
        %div= result
%div.border.border-secondary-subtle.border-1.d-flex.flex-column
    %div.profile-header.mw-100.border-bottom
        = "#{t "activerecord.models.#{attr_name.singularize}"} (#{relation_of(person, attr_name).count})"
    %div.d-flex.flex-column.ms-5.mt-3.mb-3
        = link_to "#{t "activerecord.models.#{attr_name.singularize}"} hinzufügen", new_polymorphic_path([person, attr_name.singularize.to_sym]), data: { turbo_frame: "#{dom_id class_from_string(attr_name).new}"},  method: :get
        %turbo-frame{id: attr_name}
            %div.border.rounded.border
                %turbo-frame{id: "#{dom_id  class_from_string(attr_name).new}"}
                = render relation_of(person, attr_name).list
%div.d-flex.align-items-center.justify-content-between
  %div.d-flex.col-10.gap-3
    %div.col-6
      = form_with do |f|
        %section{"data-controller": "dropdown"}
          = f.collection_select :person_id, Person.all.sort_by {|p| p.name.downcase }, :id, :name, select_when_availabale(person), {id: "dropdown", "data-action": "change->dropdown#handleChange", "data-value": "/people/"}
    %span.form-label.text-gray
      = "#{t 'profile.updated_at'}: #{@person&.last_updated_at.strftime("%d.%m.%Y")}" if @person  
  %a.d-flex.justify-content-between#new-person-button{href: "/people/new"}
    %img.pe-1{src: "/assets/plus-lg.svg"}
    Neues Profil
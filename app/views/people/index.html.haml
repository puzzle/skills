= render partial: "people/search", locals: { person: nil }
- flash.each do |type, msg|
  .alert.alert-danger.mt-3
    = msg
%div.mt-3
  %h1.d-inline-block.me-3.banner-text Willkommen bei
  =image_tag('logo.svg', width: '340', class: 'align-baseline test')
  %div.d-flex.justify-content-center.flex-column
    %h3.banner-sub-text.mt-5.fw-bold Zeit bis zum nächsten Elternabend 
    %h5.banner-sub-text.mt-1
      = distance_of_time_in_words_to_now(Date.new(2024, 10, 1))



- if flash[level].present?
  - alert_class = (flash[level] == t("devise.failure.already_authenticated")) ? "alert-success" : "alert-#{flash_class(level)}"
  %div{class: "alert #{alert_class}"}= flash[level].html_safe
version: '2'

services:

  db:
    image: postgres:9.5.7
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=cv2_development
      - POSTGRES_USER=puzzlecv
      - POSTGRES_PASSWORD=puzzlecv

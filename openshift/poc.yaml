---
apiVersion: v1
kind: ImageStream
metadata:
  name: ruby-slim-upstream
  annotations:
    puzzle.ch/purpose: Upstream image change trigger
spec:
  dockerImageRepository: docker.io
  tags:
    - from:
        kind: DockerImage
        name: docker.io/ruby:2.7-slim
      importPolicy:
        scheduled: true
      name: latest
  lookupPolicy:
    local: true
...
---
apiVersion: v1
kind: ImageStream
metadata:
  name: skills
spec:
  lookupPolicy:
    local: true
...
---
apiVersion: v1
kind: ImageStream
metadata:
  name: elixir-upstream
  annotations:
    puzzle.ch/purpose: Upstream image change trigger
spec:
  dockerImageRepository: docker.io/hexpm/elixir
  tags:
    - from:
        kind: DockerImage
        name: docker.io/hexpm/elixir:1.11.1-erlang-22.3.4.12-alpine-3.12.0
      importPolicy:
        scheduled: true
      name: latest
  lookupPolicy:
    local: true
---
apiVersion: v1
kind: BuildConfig
metadata:
  labels:
    app: skills
  name: skills
spec:
  successfulBuildsHistoryLimit: 3
  output:
    to:
      kind: ImageStreamTag
      name: skills:latest
  resources:
    limits:
      cpu: 1
      memory: 2Gi
    requests:
      cpu: 10m
      memory: 128Mi
  source:
    git:
      ref: master
      uri: https://github.com/puzzle/skills.git
    type: Git
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
      from:
        kind: ImageStreamTag
        name: ruby:2.7-slim
      env:
        - name: TZ
          value: Europe/Zurich
        - name: RAILS_ENV
          value: production
        - name: RAKE_ENV
          value: production
        - name: SECRET_KEY_BASE
          valueFrom:
            secretKeyRef:
              name: secret-key-base
              key: key
  triggers:
    - imageChange:
        from:
          kind: ImageStreamTag
          name: ruby:2.7-slim
      type: ImageChange
...
